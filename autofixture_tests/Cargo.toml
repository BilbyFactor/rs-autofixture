[package]
name = "rs-autofixture-tests"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
rs-autofixture = { path = "../autofixture", features = ["derive", "chrono"] }
chrono = "0.4"
paste = "1.0"
uuid = "1.0"
